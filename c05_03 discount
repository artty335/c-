#include <iostream>

using namespace std;

int main() {
    int n;
    cin >> n; 

    int purchaseAmount = 0;

    for (int i = 0; i < n; i++) {
        int price;
        cin >> price; 
        purchaseAmount += price; 
    }

    cout << "SUM: " << purchaseAmount << endl;

    return 0;
}
